# Elegant Item Manager Frontend

A modern, responsive frontend application for managing items, built with **React**, **Vite**, and **Vanilla CSS**.

## ✨ Features

- **Elegant Design**: Deep dark, glassmorphic UI with vibrant gradients.
- **Smooth Animations**: Powered by `framer-motion` for delightful interactions.
- **CRUD Operations**: Complete Create, Read, Update, and Delete functionality.
- **Real-time Search**: Instantly filter items by name.
- **Responsive**: Fully optimized for desktop and mobile devices.

## 🛠 Tech Stack

- **Framework**: React 18
- **Build Tool**: Vite
- **Styling**: Vanilla CSS (Variables, Flexbox/Grid)
- **Animations**: Framer Motion
- **Icons**: Lucide React
- **HTTP Client**: Axios

## 🚀 Getting Started

### Prerequisites

- Node.js installed on your machine.
- The backend server running on `http://localhost:3000` (ensure CORS is enabled).

### Installation

1.  Navigate to the project directory:
    ```bash
    cd test-frontend
    ```

2.  Install dependencies:
    ```bash
    npm install
    ```

3.  Start the development server:
    ```bash
    npm run dev
    ```

4.  Open your browser and visit the URL shown in the terminal (usually `http://localhost:5173`).

## 📦 Build for Production

To create a production-ready build:

```bash
npm run build
```

The output will be in the `dist` directory.
